# backend

API em Python (FastAPI) responsável por:

- consumir a API pública do IBGE e persistir os dados no PostgreSQL;
- expor endpoints de consulta/filtro sobre os dados já persistidos.

Ainda a implementar — ver histórico de commits e o README raiz do projeto.
